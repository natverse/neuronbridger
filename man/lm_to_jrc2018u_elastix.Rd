% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_pipeline.R
\name{lm_to_jrc2018u_elastix}
\alias{lm_to_jrc2018u_elastix}
\title{Register a native LSM volume to JRC2018U via Elastix}
\usage{
lm_to_jrc2018u_elastix(
  nc82,
  gfp,
  output_dir,
  template = NULL,
  param_files = NULL,
  elastix = "elastix",
  transformix = "transformix",
  threads = 8L,
  verbose = TRUE
)
}
\arguments{
\item{nc82}{path to native NC82 NRRD (the registration moving image).}

\item{gfp}{path to native GFP NRRD (same voxdims as \code{nc82}).}

\item{output_dir}{directory for Elastix outputs (transform params,
warped NC82, warped GFP).}

\item{template}{path to JRC2018U template NRRD (the registration
fixed image). Defaults to \code{~/templates/JRC2018_UNISEX_20x_HR.nrrd}
--- override via \code{options(neuronbridger.jrc2018u_template = ...)}.}

\item{param_files}{length-2 character of Elastix parameter file
paths (\code{p_affine.txt}, \code{p_bspline.txt}). Defaults to
the package bundle.}

\item{elastix}{path to the \code{elastix} binary.}

\item{transformix}{path to the \code{transformix} binary.}

\item{threads}{number of threads (Elastix default uses all).}
}
\value{
a list with \code{nc82_jrc2018u} (warped reference NRRD path),
  \code{gfp_jrc2018u} (warped GFP NRRD path), \code{params}
  (Elastix output directory), and \code{final_metric} (last metric
  value reported by Elastix).
}
\description{
Runs Elastix multi-resolution affine + B-spline to
align a native-confocal NC82 volume to the JRC2018U_HR template,
then applies the same transform to the matching GFP channel via
\code{transformix}. Parameter files default to the package's
\code{inst/extdata/elastix_lm_to_jrc2018u/} bundle.
}
\keyword{internal}
