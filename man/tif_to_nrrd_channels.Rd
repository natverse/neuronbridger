% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_pipeline.R
\name{tif_to_nrrd_channels}
\alias{tif_to_nrrd_channels}
\title{Split a 2-channel ImageJ-format TIFF into per-channel NRRDs}
\usage{
tif_to_nrrd_channels(
  input,
  output_dir,
  voxdims,
  prefix = NULL,
  channels = c(0L, 1L),
  labels = NULL,
  verbose = TRUE
)
}
\arguments{
\item{input}{path to a 2-channel ImageJ-format TIFF.}

\item{output_dir}{directory for output NRRDs (created if missing).}

\item{voxdims}{length-3 numeric \code{c(x, y, z)} voxel size in
microns (e.g. \code{c(0.4, 0.4, 0.4)} for a JRCVNC2018F-aligned
source).}

\item{prefix}{optional output stem (default = input file basename).}

\item{channels}{integer indices of the channels to extract
(zero-based, matching the TIFF order). Default \code{c(0L, 1L)}.}

\item{labels}{per-channel labels used in the output filenames
(\code{<stem>_<label>.nrrd}). Defaults to \code{ch0}, \code{ch1}, ...}

\item{verbose}{logical; emit a one-line shape summary.}
}
\value{
a named character vector of absolute NRRD paths, one per
  requested channel.
}
\description{
VNC analogue of \code{\link{lsm_to_nrrd}} for sources
that have already been registered into a Janelia template space and
arrive as plain ImageJ hyperstack TIFFs (no LSM metadata). The
caller passes the voxdims they know from the target template ---
the ImageJ TIFF resolution tags are typically uncalibrated for these
"registered" exports and would otherwise read as 1 px / unit.

Pure R: uses \code{tiff::readTIFF} to read every page of the
hyperstack, infers the channel/slice axis order from the
\code{ImageDescription} tag (\code{channels=}, \code{slices=}), and
writes per-channel \code{im3d} NRRDs via \code{nat::write.nrrd}.
}
\keyword{internal}
