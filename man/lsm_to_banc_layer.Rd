% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_pipeline.R
\name{lsm_to_banc_layer}
\alias{lsm_to_banc_layer}
\title{Bridge a Deng et al. 2019 LSM volume into BANC voxel coordinates}
\usage{
lsm_to_banc_layer(
  input,
  gene,
  sample,
  channel = "GFP",
  dataset = "deng_et_al_2019",
  output_dir,
  source_path = NULL,
  chunk_size = c(64L, 64L, 64L),
  keep_intermediates = FALSE,
  threads = 8L,
  verbose = TRUE
)
}
\arguments{
\item{input}{source .lsm path.}

\item{gene}{short gene name; forms layer name \code{<gene>_<sample>}.}

\item{sample}{sample identifier (e.g. \code{BRAIN-F}, \code{VNC-M},
or any user-chosen short tag).}

\item{channel}{the upstream channel string recorded in the registry
(default \code{"GFP"}).}

\item{dataset}{master folder under \code{light_level/} (default
\code{"deng_et_al_2019"}).}

\item{output_dir}{parent directory for intermediates + final
precomputed layer.}

\item{source_path}{optional original storage path for provenance.}

\item{chunk_size}{length-3 integer chunk size for the precomputed
layer.}

\item{keep_intermediates}{logical; keep the per-stage intermediate
NRRDs (default \code{FALSE}).}

\item{threads}{passed to Elastix + RenderTransformed.}
}
\value{
list with \code{precomputed_dir}, \code{registry_entry}
  (one-row tibble), \code{timings} (named numeric, s).
}
\description{
Top-level orchestrator for the Deng pipeline. Chains
Stage 0 (LSM extract) + Stage A' (Elastix native -> JRC2018U) +
Stage B'' (H5 JRC2018U -> JRC2018F) + the existing Stage C
(transformix JRC2018F -> BANC) + Stage D (nrrd_to_precomputed),
producing a Neuroglancer precomputed image layer named
\code{<gene>_<sample>.ng/} on the BANC grid.
}
\section{Software prerequisites}{

\itemize{
  \item Elastix 5.x \code{elastix} + \code{transformix} on PATH
  \item Java 17+ + the saalfeldlab \code{RenderTransformed} JAR
  \item Python (via \pkg{reticulate}) with \pkg{tifffile} +
        \pkg{SimpleITK}
  \item \pkg{nat.jrcbrains} H5 registrations downloaded
        (\code{nat.jrcbrains::download_saalfeldlab_registrations()})
  \item JRC2018U template NRRD at
        \code{~/templates/JRC2018_UNISEX_20x_HR.nrrd}
        (download from \code{janelia-flylight-templates} S3).
}
}

