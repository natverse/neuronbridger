packrat/lib*/
.Rproj.user
.Rhistory
.RData
.RDatatmp
.RDataTmp
R/analysis.R
examples
.httr-oauth
.Renviron
inst/doc
# GitHub Pages now serves https://natverse.org/neuronbridger/ from the
# `gh-pages` branch (the .github/workflows/pkgdown.yaml deploy target),
# so a locally-built `docs/` is just scratch and should not be committed.
docs/
neuronbridger/
nrrd/
mip/
results/
/.claude
.claude/
CLAUDE.md
CLAUDE.local.md
tasks.md
*.Rcheck/
*.tar.gz
vignettes/*.html
vignettes/*_files/
vignettes/*_cache/
vignettes/cache/
vignettes/figure/
# Vignette-script derived data — regenerable by inst/scripts/run_asta_sez.R.
# Cached NB API calls and ranked tables; the figures embedded in the
# vignette are kept under inst/images/ (not ignored).
inst/extdata/asta_sez/
.DS_Store
/.tmp_misplaced_hits
